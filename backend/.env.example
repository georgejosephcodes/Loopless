GEMINI_API_KEY=
GEOAPIFY_API_KEY=
ORS_API_KEY=
REDIS_URL=
PORT=5000
MONGODB_URI=
# generate each with: node -e "console.log(require('crypto').randomBytes(48).toString('hex'))"
JWT_ACCESS_SECRET=
JWT_REFRESH_SECRET=
CLIENT_ORIGIN=http://localhost:5173
# Test account demo@loopless.test / Demo@12345 is created automatically outside production.
# Set to true to force it on (or false to turn it off).
SEED_DEMO_USER=
